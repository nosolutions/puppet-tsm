* HEADER: This file was autogenerated by puppet.  Any changes to this
* HEADER: file are going to overwritten on the next puppet run.
* HEADER: If you would like to include local settings, use dsm.sys.local

* Include/exclude stuff
<%- if ! @inclexcl_hash.nil? -%>
<%-   @inclexcl_hash.keys.sort.each do |inclexcl_key| -%>
<%-     inclexcl_value = @inclexcl_hash[inclexcl_key] -%>
<%-     if inclexcl_value.kind_of?(Array) -%>
<%-       inclexcl_value.each do |item| -%>
<%-         line = "   %-21s %s" % [inclexcl_key, item] -%>
<%=     line %>
<%-       end -%>
<%-     else -%>
<%-       line = "   %-21s %s" % [inclexcl_key, inclexcl_value] -%>
<%=     line %>
<%-     end -%>
<%-   end -%>
<%- end -%>

* settings included from dsm.sys.local (if any)
