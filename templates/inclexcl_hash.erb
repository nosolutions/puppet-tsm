* HEADER: This file was autogenerated by puppet.  Any changes to this
* HEADER: file are going to overwritten on the next puppet run.

* Blank lines in this file do not indicate an error.  It just means
* that there were no includes/excludes configured/set in Hiera.

<%- if ! scope['::tsm::inclexcl_hash'].nil? -%>
<%-   scope['::tsm::inclexcl_hash'].keys.sort.each do |config_key| -%>
<%-     config_value = scope['::tsm::inclexcl_hash'][config_key] -%>
<%-     if config_value.kind_of?(Array) -%>
<%-       config_value.each do |item| -%>
<%-         line = "   %-21s %s" % [config_key, item] -%>
<%=     line %>
<%-       end -%>
<%-     else -%>
<%-       line = "   %-21s %s" % [config_key, config_value] -%>
<%=     line %>
<%-     end -%>
<%-   end -%>
<%- end -%>
